import React from 'react';

import { Layout } from '../layout';

const Home = () => 
    <Layout>
            <div class="font-bold text-5xl text-center content-center mb-2 animate-bounce">Single Page Application with React Router</div>
    </Layout>;

export default Home;